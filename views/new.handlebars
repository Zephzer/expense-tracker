{{> message}}
<div class="container mt-5">
    <div style="display: flex; justify-content:space-between;">
        <p>*必填欄位</p>
    <h2>請輸入你的支出</h2>
    </div>
    <form action="/record" method="POST">
    <div>
        <div>
            <label class="form-label" for="name" >*名稱:</label>
            <input class="mt-2 form-control" type="text" placeholder="支出名稱" value="" name="name" maxlength="10" required>
        </div>
        <div>
            <label class="form-label" for="date">*日期:</label>
            <input class="mt-2 form-control" type="date" placeholder="支出名稱" value="" name="date" required>
        </div>
        <div>
            <label class="form-label" for="category">*類別:</label>
            <select class="form-select" aria-label="Default select example" id="category" name="category">
                <option selected>類別...</option>
                <option value="交通出行">交通出行</option>
                <option value="家居物業">家居物業</option>
                <option value="休閒娛樂">休閒娛樂</option>
                <option value="餐飲食品">餐飲食品</option>
                <option value="其他">其他</option>
            </select>
        </div>
        <div>
            <label class="form-label" for="amount">*金額:</label>
            <input class="mt-2 form-control" type="number" placeholder="" value="" name="amount" maxlength="10" required>
        </div>
        <button type="submit" class="btn btn-primary mt-2">新增支出</button>
    </div>
</form>
</div>